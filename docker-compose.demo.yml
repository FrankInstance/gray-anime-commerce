services:
  gateway:
    environment:
      SPRING_PROFILES_ACTIVE: demo

  user-service:
    environment:
      SPRING_PROFILES_ACTIVE: demo
      DEMO_CLEANUP_ENABLED: "true"

  content-service:
    environment:
      SPRING_PROFILES_ACTIVE: demo

  shop-service:
    environment:
      SPRING_PROFILES_ACTIVE: demo

  inventory-service:
    environment:
      SPRING_PROFILES_ACTIVE: demo

  order-service:
    environment:
      SPRING_PROFILES_ACTIVE: demo

  payment-service:
    environment:
      SPRING_PROFILES_ACTIVE: demo
      PAYMENT_PROVIDER: demo
      PAYMENT_DEMO_ENABLED: "true"

  ingestion-service:
    environment:
      SPRING_PROFILES_ACTIVE: demo

  assistant-service:
    environment:
      SPRING_PROFILES_ACTIVE: demo
